## What does this change?

<!-- A short, plain summary of what your PR does and why. -->

## How did you test it?

<!-- e.g. `npm test`, manual `npm start` + curl, etc. -->

## Checklist

- [ ] `npm test` passes
- [ ] If I changed routes in `server.js`, I ran `npm run generate-openapi` and committed the updated `openapi.json`
- [ ] No secrets, `.env` files, `node_modules/`, browser binaries, or `.tgz` tarballs are committed
- [ ] `LICENSE` / `NOTICE.md` are untouched
